public class Stringmethod {
    public void stringmethod(){
        // string =  a reference date type that can store one 
        //           or more characters
        //           reference data types have access useful methods 
    
        String name = "Halisa";
        
        // boolean result = name.equals("halisa");

        // boolean result = name.equalsIgnoreCase("halisa");

        // int result = name.length();
        
        // char result = name.charAt(2);

        // int result = name.indexOf("l");

        // boolean result = name.isEmpty();

        // String result = name.toUpperCase();

        // String result = name.toLowerCase();

        // String name = "   Halisa   ";
        // String result = name.trim();

        String result = name.replace("s", "d");

        System.out.println(result);

        

    
    }
    
}
